# ml/bytetrack.yaml
# ByteTrack configuration for traffic detection

# Tracking parameters
track_high_thresh: 0.5
track_low_thresh: 0.1     # CRITICAL: Allows low-confidence detections
new_track_thresh: 0.6
track_buffer: 30
match_thresh: 0.8

# ByteTrack specific
track_thresh: 0.5
track_buffer: 30
match_thresh: 0.8
min_box_area: 10          # Helps with small vehicles
mot20: False